<% require 'colorscheme'
dark = ColorScheme.dark.colors_by_base
gui_scaling_factor = @facts['scaling'] ? @facts['scaling']['gui'] : 1
-%>
[global]
geometry = "<%= (300 * gui_scaling_factor).round %>x4+<%= (8 * gui_scaling_factor).round %>+<%= (28 * gui_scaling_factor).round %>"
shrink = true
transparency = 10
separator_height = <%= (1 * gui_scaling_factor).round %>
padding = <%= (4 * gui_scaling_factor).round %>
horizontal_padding = <%= (8 * gui_scaling_factor).round %>
separator_color = foreground
font = sans <%= (10 * gui_scaling_factor).round %>
markup = full
format = "<b>%s</b>\n%b"
word_wrap = true
icon_position = left
max_icon_size = <%= (48 * gui_scaling_factor).round %>
icon_path = /usr/share/icons/breeze-dark/actions/22:/usr/share/icons/breeze-dark/devices/22:/usr/share/icons/breeze-dark/status/22
dmenu = /usr/bin/rofi -dmenu -p dunst

[urgency_low]
background = "<%= dark[0x01].web %>"
foreground = "<%= dark[0x04].web %>"

[urgency_normal]
background = "<%= dark[0x01].web %>"
foreground = "<%= dark[0x06].web %>"

[urgency_critical]
background = "<%= dark[0x08].shade.web %>"
foreground = "<%= dark[0x06].web %>"
