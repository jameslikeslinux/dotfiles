#!/usr/bin/env zsh

SELF="$0"
source "$(dirname "$(readlink -f "$SELF")")/lib.zsh" "$SELF"

get_state > /dev/null && sleep 5
read load rest < /proc/loadavg
first_segment "$(fontawesome "\uf233") ${load}"
set_state 1
