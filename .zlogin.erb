# Configure color palette on Linux console
<% require 'colorscheme'
c = ColorScheme.new
linux_escapes = c.console.map do |color|
  '\e]P%x%s' % [color.ansi, color.hex]
end.join -%>
if [[ $TERM == 'linux' ]]; then
    print -Pn '<%= linux_escapes %>'
    clear
fi
