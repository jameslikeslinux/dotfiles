#!/usr/bin/env zsh
#
# alacrity wrapper to force X11
#
# The Xwayland clipboard is only accessible when an Xwayland window is focused,
# so make alacritty run under X11 to ensure the clipboard is available to
# remote SSH sessions.
#
export WINIT_UNIX_BACKEND=x11
exec /usr/bin/alacritty "$@"
